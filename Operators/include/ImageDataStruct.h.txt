#pragma once

#include "Pixle.h"
#include "Macros.h"
#include "Image.h"

class ImageDataStructure {
public:
	ImageDataStructure::ImageDataStructure() {}
	ImageDataStructure(int, int, unsigned char);
	unsigned int operator()(int, int) const;
	unsigned int operator()(int, int);
	void free(int);
private:
	Pixel** m_arr;
};

